description.pattern=<a href="{0}">Fileset 'includes'</a> \
                 setting that specifies the generated raw CSSLint XML report files in Checkstyle format, \
                 such as '**/csslint-result.xml'. Basedir of the fileset is <a href="ws/">the workspace root</a>. \
                 If no value is set, then the default '**/csslint-result.xml' is used. Be sure not to include any \
                 non-report files into this pattern.
